package org.scobca.loggerservice.enums

/**
 * Enum class representing Kafka topics used in the logging service.
 *
 * This enum centralizes the definition of Kafka topic names to avoid
 * hardcoding string literals throughout the code.
 */
enum class KafkaTopics {
    LOGGER_EVENTS_TOPIC,
}