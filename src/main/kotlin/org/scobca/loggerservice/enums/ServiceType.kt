package org.scobca.loggerservice.enums

/**
 * Enum class representing types of services within the system.
 *
 * Each enum constant corresponds to a distinct service that interacts
 * with the logging infrastructure or business logic.
 */
enum class ServiceType {
    USERS_SERVICE,
    EVENTS_SERVICE,
    FILES_SERVICE,
    MAIL_SERVICE,
}